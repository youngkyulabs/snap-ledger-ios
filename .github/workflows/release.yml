name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Metadata · Screenshots · Build link
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      ASC_KEY_PATH: ${{ github.workspace }}/.asc_key.p8
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      TAG_WORKFLOW_ID: ${{ vars.XCODE_CLOUD_TAG_WORKFLOW_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Restore ASC private key
        env:
          ASC_PRIVATE_KEY: ${{ secrets.ASC_PRIVATE_KEY }}
        run: |
          printf '%s' "$ASC_PRIVATE_KEY" > "$ASC_KEY_PATH"
          chmod 600 "$ASC_KEY_PATH"

      - name: Unit tests for release scripts
        run: python3 -m unittest discover -s scripts/tests -t . -v

      - name: Preflight (blocks before anything is uploaded)
        run: |
          python3 -m scripts.asc_release preflight \
            --tag "${GITHUB_REF_NAME}" \
            --workflow-id "${TAG_WORKFLOW_ID}"

      - name: Audit metadata against App Store Connect
        continue-on-error: true
        run: python3 -m scripts.asc_release audit

      - name: Stage screenshots
        run: ./scripts/stage_screenshots.sh

      - name: Set up Ruby and fastlane
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'

      - name: Install fastlane
        run: gem install fastlane -N -v 2.240.1

      - name: Build ASC API key JSON for fastlane
        run: |
          python3 - <<'PY'
          import json, os
          with open(os.environ["ASC_KEY_PATH"], encoding="utf-8") as handle:
              key = handle.read()
          payload = {
              "key_id": os.environ["ASC_KEY_ID"],
              "issuer_id": os.environ["ASC_ISSUER_ID"],
              "key": key,
              "in_house": False,
          }
          with open(".asc_key.json", "w", encoding="utf-8") as handle:
              json.dump(payload, handle)
          PY
          chmod 600 .asc_key.json

      - name: Upload metadata and screenshots
        env:
          LANG: en_US.UTF-8
          LC_ALL: en_US.UTF-8
          FASTLANE_SKIP_UPDATE_CHECK: '1'
          FASTLANE_OPT_OUT_USAGE: '1'
        run: |
          set -euo pipefail
          fastlane deliver \
            --api_key_path .asc_key.json \
            --app_identifier com.youngkyu.snapledger \
            --app_version "${GITHUB_REF_NAME#v}" \
            --metadata_path ./fastlane/metadata \
            --screenshots_path ./fastlane/screenshots \
            --overwrite_screenshots true \
            --skip_binary_upload true \
            --submit_for_review false \
            --precheck_include_in_app_purchases false \
            --force

      - name: Wait for the Xcode Cloud build and link it
        run: |
          python3 -m scripts.asc_release link-build \
            --tag "${GITHUB_REF_NAME}" \
            --commit "${GITHUB_SHA}" \
            --workflow-id "${TAG_WORKFLOW_ID}"

      - name: Remove credentials
        if: always()
        run: rm -f "$ASC_KEY_PATH" .asc_key.json
